import { LargoMaximoDirective } from './largo-maximo.directive';

describe('LargoMaximoDirective', () => {
  it('should create an instance', () => {
    const directive = new LargoMaximoDirective();
    expect(directive).toBeTruthy();
  });
});
