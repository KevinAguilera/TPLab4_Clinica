import { FechaDirectivaDirective } from './fecha-directiva.directive';

describe('FechaDirectivaDirective', () => {
  it('should create an instance', () => {
    const directive = new FechaDirectivaDirective();
    expect(directive).toBeTruthy();
  });
});
