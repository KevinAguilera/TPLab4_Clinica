import { TurnoExistentePipe } from './turno-existente.pipe';

describe('TurnoExistentePipe', () => {
  it('create an instance', () => {
    const pipe = new TurnoExistentePipe();
    expect(pipe).toBeTruthy();
  });
});
